# Backend API base URL (no trailing slash). Leave empty for same-origin (dev with Vite proxy).
# When frontend and backend are deployed separately, set to your backend URL, e.g.:
# VITE_API_BASE_URL=https://your-backend.railway.app
# VITE_API_BASE_URL=https://pokerpsych-api.vercel.app
# VITE_API_BASE_URL=
